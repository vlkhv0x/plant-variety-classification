# 🚀 Быстрый старт

## Вариант 1: Демо с синтетическими данными (без скачивания датасета)

Для быстрой проверки работы проекта:

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите демо
python demo.py
```

Это создаст синтетические данные и обучит модель на 3 эпохах для демонстрации.

---

## Вариант 2: Полное обучение на реальных данных

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Скачивание датасета

#### Вариант A: Через Kaggle API (рекомендуется)

```bash
# Установите Kaggle API
pip install kaggle

# Настройте API ключ (получите на kaggle.com/account)
mkdir ~/.kaggle
mv kaggle.json ~/.kaggle/
chmod 600 ~/.kaggle/kaggle.json

# Скачайте датасет
kaggle datasets download -d crackheadseedz/seed-image-dataset
unzip seed-image-dataset.zip -d data/raw/seeds/
```

#### Вариант B: Ручное скачивание

1. Перейдите на: https://www.kaggle.com/datasets/crackheadseedz/seed-image-dataset
2. Скачайте датасет
3. Распакуйте в `data/raw/seeds/`

**Ожидаемая структура:**
```
data/raw/seeds/
├── wheat/
│   ├── image1.jpg
│   ├── image2.jpg
│   └── ...
├── corn/
├── barley/
└── ...
```

### Шаг 3: Обучение модели

```bash
# Базовое обучение (50 эпох)
python src/train.py --epochs 50 --batch_size 32

# С fine-tuning
python src/train.py --epochs 50 --fine_tune --fine_tune_epochs 20

# С другой моделью
python src/train.py --model_type efficientnet --epochs 50
```

### Шаг 4: Оценка модели

```bash
python src/evaluate.py --model_path models/best_model.h5
```

### Шаг 5: Предсказание на новых изображениях

```bash
python src/predict.py --image_path path/to/seed.jpg --show_image
```

---

## Использование Jupyter Notebook

```bash
# Запуск Jupyter
jupyter notebook notebooks/plant_classification.ipynb
```

Notebook содержит весь процесс с подробными комментариями и визуализациями.

---

## Параметры командной строки

### train.py

- `--data_dir` - путь к данным (по умолчанию: `data/raw/seeds`)
- `--epochs` - количество эпох (по умолчанию: 50)
- `--batch_size` - размер батча (по умолчанию: 32)
- `--img_size` - размер изображения (по умолчанию: 224)
- `--learning_rate` - learning rate (по умолчанию: 0.0001)
- `--model_type` - тип модели: `resnet50`, `efficientnet`, `vgg16`
- `--fine_tune` - выполнить fine-tuning
- `--fine_tune_epochs` - эпох для fine-tuning (по умолчанию: 20)

### evaluate.py

- `--model_path` - путь к модели (по умолчанию: `models/best_model.h5`)
- `--data_dir` - путь к данным
- `--batch_size` - размер батча
- `--img_size` - размер изображения

### predict.py

- `--image_path` - путь к изображению (обязательный)
- `--model_path` - путь к модели
- `--config_path` - путь к конфигурации
- `--top_k` - количество топ предсказаний (по умолчанию: 3)
- `--show_image` - показать визуализацию

---

## Типичные проблемы

### GPU не обнаружен

```python
# Проверьте GPU
import tensorflow as tf
print(tf.config.list_physical_devices('GPU'))

# Если пусто - TensorFlow использует CPU (медленнее, но работает)
```

### Ошибка памяти

Уменьшите `batch_size`:
```bash
python src/train.py --batch_size 16  # или 8
```

### Датасет не найден

Проверьте структуру:
```bash
ls -la data/raw/seeds/
```

Должны быть папки с классами, в каждой - изображения.

---

## Ожидаемые результаты

На реальном датасете семян:
- **Accuracy**: 90-95%
- **F1-Score**: 90-94%
- **Время обучения**: 30-60 минут (с GPU), 3-5 часов (CPU)

---

## Следующие шаги

После успешного обучения:

1. Проверьте `reports/` - там все визуализации
2. Изучите confusion matrix для анализа ошибок
3. Попробуйте fine-tuning для улучшения результатов
4. Используйте модель для предсказаний на новых данных

---

## Помощь

Если возникли проблемы:
1. Проверьте версии зависимостей: `pip list`
2. Проверьте структуру данных
3. Прочитайте полный README.md
4. Запустите demo.py для проверки окружения
